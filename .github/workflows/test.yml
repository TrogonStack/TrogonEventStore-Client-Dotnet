name: Integration Tests

on:
  workflow_dispatch:
    inputs:
      server_image:
        description: Fully qualified TrogonEventStore container image
        required: true
        type: string
      registry:
        description: Registry host for a private image; requires REGISTRY_USERNAME and REGISTRY_TOKEN secrets
        required: false
        type: string
      framework:
        description: Target framework
        required: true
        type: choice
        default: net10.0
        options: [net8.0, net9.0, net10.0]

permissions:
  contents: read

jobs:
  test:
    name: integration/${{ inputs.framework }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Install .NET SDKs
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: |
            8.0.x
            9.0.x
            10.0.x
      - name: Log in to container registry
        if: ${{ inputs.registry != '' }}
        uses: docker/login-action@v3
        with:
          registry: ${{ inputs.registry }}
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_TOKEN }}
      - name: Run integration tests
        env:
          TESTCONTAINER_KURRENTDB_IMAGE: ${{ inputs.server_image }}
        run: |
          sudo ./gencert.sh
          dotnet test test/KurrentDB.Client.Tests \
            --configuration Release \
            --framework "${{ inputs.framework }}" \
            --blame \
            --logger "GitHubActions;report-warnings=false" \
            --logger "console;verbosity=normal"
